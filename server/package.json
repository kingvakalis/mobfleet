{
  "name": "phone-farm-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "build": "prisma generate --schema=prisma/schema.postgres.prisma",
    "start:prod": "prisma db push --schema=prisma/schema.postgres.prisma --skip-generate --accept-data-loss && tsx src/index.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/websocket": "^11.0.0",
    "@prisma/client": "^6.7.0",
    "dotenv": "^17.0.0",
    "fastify": "^5.2.0",
    "phone-farm-app": "file:..",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.0",
    "prisma": "^6.7.0",
    "tsx": "^4.19.0",
    "typescript": "~5.7.0"
  }
}
