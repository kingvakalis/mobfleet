# Local secrets — never commit (.env.example is the committed template)
.env
.env.local
.env.*.local

# Compiled bundle (produced by `npm run build` / the Docker build)
dist/

# Local SQLite dev databases
*.db
*.db-journal
prisma/*.db
prisma/*.db-journal

node_modules/
